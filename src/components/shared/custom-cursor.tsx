
'use client';

// This component has been intentionally left blank to disable the custom cursor,
// which was causing critical UI interaction bugs.

export default function CustomCursor() {
  return null;
}
